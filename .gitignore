# OS
.DS_Store

# Node / frontend
node_modules/
dist/
*.log
pnpm-debug.log*

# Rust / Tauri (if used)
target/
src-tauri/target/
src-tauri/gen/schemas/

# Bundled ffmpeg/ffprobe — fetched per build platform (see scripts/fetch-ffmpeg.sh).
# GPL static builds, not committed; ship THIRD_PARTY_NOTICES alongside them.
src-tauri/binaries/

# Env / local secrets
.env
.env.*
!.env.example

# Claude Code dev tooling (machine-specific; includes vendored/symlinked skills).
# Kept local, out of the public repo.
.claude/
.agents/

# Local-only docs (developer thinking / process; NOT part of the open-source repo).
# Committed specs live at specs/ root (DESIGN.md, ...) and specs/tech_docs/.
specs/prd/
specs/research/

# Editors
.vscode/
.idea/
